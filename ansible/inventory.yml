---
# Inventory - Premier setup avec user ubuntu temporaire
# Après setup, modifier pour henri + port 2222

all:
  children:
    production:
      hosts:
        eschool-prod:
          ansible_host: 57.129.104.225
          # ansible_user: ubuntu
          # ansible_port: 22
          ansible_user: henri
          ansible_port: 2222
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          ansible_become: yes
          ansible_become_method: sudo
